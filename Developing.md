# Getting Started

```
npm install -f firebase-tools mocha
npm install
firebase login
firebase init
firebase use -add <your-project-id>
firebase emulators:start # wait for emulators to install

firebase functions:secrets:set OPENAI_API_KEY

```